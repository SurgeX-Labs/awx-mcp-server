apiVersion: v1
kind: Secret
metadata:
  name: {{ include "awx-mcp-server.fullname" . }}
  labels:
    {{- include "awx-mcp-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  admin-token: {{ .Values.secrets.adminToken | quote }}
